version : 2

sources:
  - name: raw
    schema: gz_raw_data
    database: fourth-buffer-484809-j2
    loader: bigquery
    tables: 
      - name: raw_gz_product
        identifier: product
      - name: raw_gz_sales
        identifier: sales
      - name: raw_gz_ship
        identifier: ship